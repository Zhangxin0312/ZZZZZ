package com.bw.myproduct.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bw.myproduct.beans.GouBeans;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GOU_BEANS".
*/
public class GouBeansDao extends AbstractDao<GouBeans, Long> {

    public static final String TABLENAME = "GOU_BEANS";

    /**
     * Properties of entity GouBeans.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Message = new Property(1, String.class, "message", false, "MESSAGE");
        public final static Property Status = new Property(2, String.class, "status", false, "STATUS");
        public final static Property Flag = new Property(3, boolean.class, "flag", false, "FLAG");
        public final static Property CommodityId = new Property(4, int.class, "commodityId", false, "COMMODITY_ID");
        public final static Property CommodityName = new Property(5, String.class, "commodityName", false, "COMMODITY_NAME");
        public final static Property Count = new Property(6, int.class, "count", false, "COUNT");
        public final static Property Pic = new Property(7, String.class, "pic", false, "PIC");
        public final static Property Price = new Property(8, int.class, "price", false, "PRICE");
        public final static Property Num = new Property(9, int.class, "num", false, "NUM");
    }


    public GouBeansDao(DaoConfig config) {
        super(config);
    }
    
    public GouBeansDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GOU_BEANS\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MESSAGE\" TEXT," + // 1: message
                "\"STATUS\" TEXT," + // 2: status
                "\"FLAG\" INTEGER NOT NULL ," + // 3: flag
                "\"COMMODITY_ID\" INTEGER NOT NULL ," + // 4: commodityId
                "\"COMMODITY_NAME\" TEXT," + // 5: commodityName
                "\"COUNT\" INTEGER NOT NULL ," + // 6: count
                "\"PIC\" TEXT," + // 7: pic
                "\"PRICE\" INTEGER NOT NULL ," + // 8: price
                "\"NUM\" INTEGER NOT NULL );"); // 9: num
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GOU_BEANS\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GouBeans entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String message = entity.getMessage();
        if (message != null) {
            stmt.bindString(2, message);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(3, status);
        }
        stmt.bindLong(4, entity.getFlag() ? 1L: 0L);
        stmt.bindLong(5, entity.getCommodityId());
 
        String commodityName = entity.getCommodityName();
        if (commodityName != null) {
            stmt.bindString(6, commodityName);
        }
        stmt.bindLong(7, entity.getCount());
 
        String pic = entity.getPic();
        if (pic != null) {
            stmt.bindString(8, pic);
        }
        stmt.bindLong(9, entity.getPrice());
        stmt.bindLong(10, entity.getNum());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GouBeans entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String message = entity.getMessage();
        if (message != null) {
            stmt.bindString(2, message);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(3, status);
        }
        stmt.bindLong(4, entity.getFlag() ? 1L: 0L);
        stmt.bindLong(5, entity.getCommodityId());
 
        String commodityName = entity.getCommodityName();
        if (commodityName != null) {
            stmt.bindString(6, commodityName);
        }
        stmt.bindLong(7, entity.getCount());
 
        String pic = entity.getPic();
        if (pic != null) {
            stmt.bindString(8, pic);
        }
        stmt.bindLong(9, entity.getPrice());
        stmt.bindLong(10, entity.getNum());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public GouBeans readEntity(Cursor cursor, int offset) {
        GouBeans entity = new GouBeans( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // message
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // status
            cursor.getShort(offset + 3) != 0, // flag
            cursor.getInt(offset + 4), // commodityId
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // commodityName
            cursor.getInt(offset + 6), // count
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // pic
            cursor.getInt(offset + 8), // price
            cursor.getInt(offset + 9) // num
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, GouBeans entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMessage(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setStatus(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFlag(cursor.getShort(offset + 3) != 0);
        entity.setCommodityId(cursor.getInt(offset + 4));
        entity.setCommodityName(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setCount(cursor.getInt(offset + 6));
        entity.setPic(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setPrice(cursor.getInt(offset + 8));
        entity.setNum(cursor.getInt(offset + 9));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(GouBeans entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(GouBeans entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(GouBeans entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
